# default install: the complete reproducible baseline. BM25 and TF-IDF
# vector embedding/indexing, the grounded extractive answerer, every eval
# script and every test that scores them. Installs in seconds, no torch,
# no model downloads, no network at runtime.
# Tested with Python 3.9.6 and 3.12.13.
#
# Optional extras (each with committed evaluation outputs):
#   requirements-dense.txt  MiniLM dense + hybrid retrieval ablation (torch)
#   requirements-gen.txt    local generative answerer experiment (torch)
#   requirements-lock.txt   exact versions of the environment that produced
#                           the committed outputs
scikit-learn>=1.3,<2
numpy>=1.24,<3

# corpus fetching (only needed to re-run data/fetch_corpus.py; the corpus is
# already committed under data/corpus so a clean clone does not need these)
# requests>=2.28
# beautifulsoup4>=4.11
