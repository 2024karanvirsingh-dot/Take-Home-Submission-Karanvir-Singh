# core pipeline
scikit-learn>=1.3
numpy>=1.24

# corpus fetching (only needed to re-run data/fetch_corpus.py; the corpus is
# already committed under data/corpus so a clean clone does not need these)
requests>=2.28
beautifulsoup4>=4.11

# optional: generation backend. only needed if you set ANTHROPIC_API_KEY to get
# real generated answers instead of the extractive fallback.
anthropic>=0.39
