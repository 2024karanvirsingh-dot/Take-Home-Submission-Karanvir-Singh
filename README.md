# RAG over the UCMJ with grounded answer synthesis and honest evaluation

A retrieval augmented question answering pipeline over the Uniform Code of
Military Justice, with a fully local, deterministic, citation constrained
answer layer and an evaluation that scores its own outputs and digs into
where and why they break. The default install is lightweight and runs in
seconds from a clean clone; a dense retrieval ablation and a small local
generative experiment are optional extras whose evaluation outputs are
committed. There is no API call and no credential anywhere in this
repository. The failure analysis matters more than the pipeline, so most of
this README is about the failures.

## Executive summary

- **Corpus**: the statutory text of 10 U.S.C. chapter 47 (the UCMJ), all
  198 provisions in force, committed under `data/corpus/`; 572 chunks at
  the default chunking.
- **Questions**: 18 in `eval/questions.json` across eight types (numeric,
  limitations, definition, enumeration, conditional, factual lookup, two
  comparative variants), 16 answerable from the corpus plus 2 boundary
  controls that must be refused.
- **Default system** (clean clone, no downloads): BM25 retrieval with
  article number routing, TF-IDF vector embedding baseline alongside it,
  grounded extractive answer synthesis. Retrieval hit@1 9/16, MRR 0.62;
  answers: 13/16 answered, 11/13 citing a gold article, citation precision
  0.577, both controls refused correctly, 18/18 verbatim support.
- **Best measured configuration** (optional dense extra, outputs
  committed): hybrid BM25 + MiniLM under reciprocal rank fusion with the
  same grounded answerer. Retrieval hit@5 14/16, MRR 0.71, all gold
  articles retrieved 14/16 (the only configuration covering both sides of
  both comparative questions); answers: 15/16 answered, 13/15 citing a
  gold article, citation precision 0.767, both controls refused, 18/18
  verbatim support.
- **Most important discovered failure**: with the safety policy removed,
  the same retrieval produces confident answers that quote genuine statute
  with real citations while attributing them to the wrong legal instrument
  (q17 quotes non-judicial punishment day limits for a court-martial
  confinement question). Every quoted sentence is real law and the
  composition is still wrong; the grounded policy is what prevents it. The
  optional generative experiment goes one step further and invents a
  punishment figure on the same question.
- **Reproduce the baseline**: `pip install -r requirements.txt && ./run_all.sh`
  (seconds; optional stages skip with a notice unless their extras are
  installed).

## Assignment compliance checklist

| requirement | where |
| --- | --- |
| real corpus ingestion | 198 UCMJ provisions in `data/corpus/`, fetched by `data/fetch_corpus.py` (committed, no network needed) |
| chunking | `rag/chunk.py`, paragraph aware windows, never crossing a provision; pinned by `tests/test_chunking.py` |
| embedding + indexing | core: TF-IDF vectors over all 572 chunks, queries embedded and matched by cosine (`rag/index.py` `TfidfCosine`); optional: MiniLM dense embeddings, same interface (`DenseEmbed`), plus BM25 inverted statistics and RRF fusion |
| 15+ questions answered with retrieved context | 18 questions, `eval/questions.json`, run by `eval/run_eval.py` |
| question specific rubric | `eval/rubric.md` |
| 3+ successes analysed | "Cases where it worked" below |
| 3+ failures analysed | "Where it failed" below (six mechanisms) |
| one component changed, before/after | answer policy ablation plus retriever and answerer swaps; "Before-and-after examples" below |
| clean clone setup | Quick start; CI runs it from scratch on every push |
| tests | 58 pytest cases, `tests/` |
| reproducible outputs | `./run_all.sh` regenerates every file in `outputs/`; tables printed by `eval/build_summary.py` |
| per question report | `outputs/evaluation_report.md`, `outputs/question_results.csv` (generated by `eval/build_report.py`) |

## Key findings

- Sparse retrieval is highly interpretable and strong when the query shares
  rare vocabulary with one provision, and an explicit article number router
  makes exact identifiers reliable for every retriever.
- Dense retrieval (optional, MiniLM) fixes the vocabulary mismatch failures
  the sparse analysis predicts (morphology, boilerplate, colloquial names)
  but loses the exact identifier signal sparse matching gets for free.
- Hybrid rank fusion is the best measured retrieval configuration: it
  trades dense's rank peaks for the best coverage floor, is the only
  configuration that fully covers both comparative questions, and converts
  two of the baseline's three false refusals into correct answers. It
  still does not dominate every query.
- A conservative grounded answer policy converts unsupported retrieval into
  visible refusals instead of unsupported legal claims. The permissive
  policy, same code minus the guardrails, answers every question and cites
  the wrong legal instrument on exactly the questions where that is most
  dangerous.
- The optional generative experiment (flan-t5-small over identical
  retrieval) answers everything, cites nothing, and invents a punishment
  figure on the boundary control; the automatic support and citation
  checks quantify all of it. It is the measured argument for why the
  extractive layer is the default.
- Two failures survive every retriever tested (q12, and the q02 paragraph
  selection miss), because they are not representational failures at all.

## Quick start

The default install is the complete reproducible baseline: pure Python plus
scikit-learn, installs in seconds, no torch, no model downloads, no network
at runtime. Tested with Python 3.9.6 and 3.12.13.

```bash
git clone https://github.com/2024karanvirsingh-dot/Take-Home-Submission-Karanvir-Singh.git
cd Take-Home-Submission-Karanvir-Singh
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# ask one question (local, deterministic, no credentials)
python3 -m rag.pipeline "What rights does Article 31 provide?"

# run the evaluation
python3 -m eval.run_eval --tag bm25_baseline

# reproduce every run, score, report and table in this README
./run_all.sh

# tests (dense and generative tests skip themselves without the extras)
pip install -r requirements-dev.txt && python3 -m pytest tests/ -q
```

Optional extras, each isolated behind its own requirements file and each
with committed evaluation outputs in `outputs/`, so reviewing their results
requires no install:

```bash
# dense (MiniLM) + hybrid retrieval ablation: pulls in torch, downloads a
# ~90 MB encoder on first use
pip install -r requirements-dense.txt
python3 -m eval.run_eval --retriever hybrid --tag hybrid_baseline

# generative answerer experiment: flan-t5-small, ~300 MB download, CPU
pip install -r requirements-gen.txt
python3 -m eval.run_eval --retriever hybrid --answerer generative --tag hybrid_generative
```

Nothing degrades silently: asking for `--retriever dense/hybrid` or
`--answerer generative` without the matching extra fails with install
instructions rather than substituting a different system, and `run_all.sh`
prints a notice for every stage it skips.

## System selection

Three clearly separated tiers, so there is no ambiguity about what runs
where:

1. **Default system (the reproducible baseline)**: BM25 retrieval with
   article number routing, feeding grounded extractive answer synthesis.
   Chosen as the default because every ranking decision and every answer
   sentence is inspectable (the eval runner prints the exact terms that
   made a chunk win), it installs and runs in seconds from a clean clone,
   and its faithfulness to retrieved context is structural rather than
   behavioral. The TF-IDF cosine retriever in the same module is the
   default install's vector embedding representation, evaluated alongside
   BM25 in the ablation.
2. **Best measured configuration (optional dense extra)**: hybrid
   BM25 + MiniLM under reciprocal rank fusion, same routing, same grounded
   answerer. It has the best coverage of the configurations tested
   (all gold retrieved 14/16, both comparative questions fully covered,
   citation precision 0.767 versus the baseline's 0.577) and converts two
   of the baseline's three false refusals into correct answers. Its
   committed run and score files are in `outputs/`. In a deployment where
   a torch dependency and a 90 MB encoder are acceptable, this is the
   configuration the measurements support; dense alone has better mean
   rank (MRR 0.79 versus 0.71) but a worse coverage floor, and in a legal
   setting a comparison answered from one side is wrong even when its one
   citation is right.
3. **Generative experiment (optional, deliberately small)**: flan-t5-small
   generating from the retrieved chunks only. Included to measure what a
   small local generator does under the same harness, not as a candidate
   default; the results (0/16 answers citing a gold article, an invented
   figure on the boundary control) are the quantified reason the
   deterministic extractive layer remains the answerer of both real
   tiers.

Remaining known failures of the best measured configuration: q04 and q12
answered with wrong citations (topically adjacent provisions passed the
lexical support test after a retrieval miss), q07 a false refusal (right
article retrieved, wrong chunk part for the enumeration gate). All three
are documented in the failure analysis and the per question report.

A production legal environment might choose differently again: BM25 only
where full audit interpretability of ranking is a requirement, a legal
domain encoder rather than general purpose MiniLM, and entailment based
support checking before any generative layer faces users.

## Architecture

```
UCMJ source documents (198 provisions, committed under data/corpus/)
        |
provision aware parsing (data/fetch_corpus.py, run once, output committed)
        |
paragraph aware chunking, never crossing a provision (rag/chunk.py)
        |
BM25 stats + TF-IDF vectors [+ optional MiniLM embedding matrix] (rag/index.py)
        |
top k retrieval (sparse, dense cosine, or RRF fusion) + article routing
(rag/pipeline.py)
        |
answer layer: grounded extractive synthesis (rag/answer.py, default)
              [or the optional generative layer, rag/generate.py]
        |
retrieval, citation, content, support, number and refusal metrics (eval/)
```

## Why this corpus

The corpus is the UCMJ, the statutory text of 10 U.S.C. chapter 47, all 198
provisions in force, split into its two natural document types:

* **93 punitive articles** (Subchapter X, Articles 77 to 134): the offenses.
  Desertion, AWOL, insubordination, murder, conduct unbecoming, the general
  article.
* **105 procedural articles** (everything else): jurisdiction, apprehension,
  non-judicial punishment, court-martial composition, trial procedure,
  sentencing, appeals.

I picked it for three reasons. First, it is the body of law I know the
context around, which matters when you are writing the answer key. Second,
it has structural traps that make retrieval evaluation interesting: every
provision has two numbers (Article 86 is 10 U.S.C. 886, the famous "Article
15" is section 815), practitioners use colloquial vocabulary the statute
never uses ("panel", "AWOL", even "UCMJ" itself), and the punitive articles
share heavy boilerplate ("Any person subject to this chapter who...") that
erodes term statistics. Third, it has a hard, well defined knowledge
boundary: the statute defines offenses but the actual punishment tables live
in the Manual for Courts-Martial, which is deliberately not in the corpus.
That boundary gives the evaluation a built in fabrication test with real
stakes.

Everything is fetched from the Cornell Legal Information Institute by
`data/fetch_corpus.py` and committed under `data/corpus/`, so a clean clone
needs no network. The fetch script exists for provenance and is never run
during evaluation.

## Ingestion and chunking

Each provision is one file. Most are short enough to be a single chunk. The
long ones (Art. 2 persons subject to the code, Art. 15 non-judicial
punishment, Art. 120 sexual offenses) are split on paragraph boundaries into
windows of about 180 words with 40 words of overlap. Chunking never crosses
a provision boundary, so every chunk maps to exactly one article and every
citation is honest. This gives 572 chunks at the default size.
`tests/test_chunking.py` pins both properties.

## Sparse, dense and hybrid indexing

The system implements and evaluates three chunk representations plus a
fusion. The first two are the default install; the last two are the
optional dense extra:

* **BM25** (`rag/index.py`), implemented from scratch (about 40 lines)
  rather than pulled from a library, so term frequency saturation and length
  normalisation are visible and the eval runner can print the exact terms
  that made a chunk win. This is the default retriever and the diagnostic
  baseline the failure analysis is built on.
* **TF-IDF cosine** via scikit-learn: the default install's vector
  embedding representation. Every chunk is embedded into a TF-IDF vector
  and indexed in a matrix at build time; each incoming question is
  embedded by the same vectorizer and matched against every chunk by
  cosine similarity.
* **Dense semantic embeddings** (`--retriever dense`, optional):
  `sentence-transformers/all-MiniLM-L6-v2` embeds every chunk (title
  prepended) into a 384 dimensional L2 normalised vector, stacked into a
  matrix at index build time. Each question is embedded by the same
  encoder and scored against every chunk by cosine (an exact matrix vector
  scan; at 572 chunks an approximate nearest neighbour index would be
  overhead, and exactness keeps the ablations clean). Embedding shape and
  normalisation are pinned by `tests/test_hybrid_and_dense.py`. This
  retriever exists to test the failure analysis's own prediction that the
  vocabulary gap misses need a representation that knows synonymy.
* **Hybrid** (`--retriever hybrid`, optional): reciprocal rank fusion of
  the BM25 and dense rankings, 1/(60+rank) summed over both top 50 lists,
  a standard fusion approach chosen for its empirical robustness across
  collections rather than tuned for this one. Deterministic tie break by
  chunk id. This is the best measured retrieval configuration.

**Article number routing** (`rag/pipeline.py`): practitioners ask about
provisions by number, but a provision's own text never states its own
number, so no retriever, sparse or dense, can find "Article 31" from the
query terms. When the question names an article the retriever did not
surface, the router swaps that article's best chunk into the last result
slot. It runs identically for every retriever, so the ablations stay a fair
comparison. Its effect is visible in the dense results below.

Tokenisation notes: the stopword list stops function words including the
ones questions contribute ("about", "does"), after a recorded failure where
those two words decided a top retrieval slot; legally loaded common words
(subject, right, order, charge, court, person) are deliberately kept. This
fix changed the retrieval baseline relative to earlier commits, and
`tests/test_retrieval.py` pins the regression. An optional light stemmer
(`--stem`) is evaluated in the ablations, where its collision costs are
measured rather than assumed.

## Answer construction

The default answer layer performs retrieval augmented answer synthesis
deterministically: it composes answers exclusively out of sentences from
the retrieved chunks, which makes support auditable by string comparison
instead of trust. A small generative layer exists as an optional,
isolated experiment, evaluated under the same harness. The two are
swappable with `--answerer extractive|generative` over identical retrieval.

### Grounded extractive synthesis (default)

`rag/answer.py`. There is no model in this path. Given the question and the
top k chunks it:

1. splits the chunks into statutory sentences and list items (reflowing the
   source's mid sentence line breaks, dropping amendment history notes);
2. scores each sentence with transparent lexical features: pool idf weighted
   token overlap (with plural folding), title matches that cover most of a
   title, bonuses for exact article number matches, quantities when the
   question asks for one, definitional clauses for definition questions and
   operative legal verbs (shall, may not, prohibited, punished);
3. applies a support test per sentence (the grounded policy): at least two
   topical shared tokens, or a rare anchor token, or the question named the
   sentence's article, or a definitional clause for a definitional question.
   Punishment amount questions additionally require punitive article
   provenance, because procedural provisions like Art. 15 are full of day
   limits that belong to a different legal instrument;
4. selects a small non duplicative set (comparative questions reserve slots
   for both sides), completes enumerations so list items survive extraction
   whole, and composes the answer with one citation per article group;
5. refuses with a standardized message when nothing clears support, states
   the Manual for Courts-Martial boundary when a punishment amount question
   finds only "as a court-martial may direct", and recognises Rules for
   Courts-Martial / MCM questions as out of corpus before retrieval is even
   consulted.

Every sentence in every answer is a verbatim extract of a retrieved chunk,
so faithfulness to the retrieved context holds by construction, and
`eval/score_answers.py` re verifies it independently anyway. The honest
consequence: this layer cannot generate novel unsupported wording, although
it can still misattribute genuine retrieved passages (quoting the wrong
article fluently), so the residual answer risk is misattribution and the
metrics below are designed to catch exactly that. Note the distinct notions
in play: verbatim extraction guarantees faithfulness to the retrieved
context, not legal correctness of the composed answer, and a correct
looking citation does not guarantee the right provision was cited. The
metrics keep these separate.

The answer layer performs one vocabulary normalisation (the statute never
calls itself "the UCMJ"; the token maps to "chapter" for ranking only, at a
fixed small weight, and never counts toward support). Retrieval does not get
that mapping; the vocabulary gap it causes is a core finding below.

### Generative layer (optional experiment)

`rag/generate.py`: google/flan-t5-small (80M parameters, ~300 MB download,
CPU, `pip install -r requirements-gen.txt`) writes the answer conditioned
on the retrieved chunks and nothing else. The prompt contains only the
retrieved statute text with article labels, instructs inline citations and
a fixed refusal marker when the context does not contain the answer.
Decoding is greedy with a fixed token budget, so the same question, chunks
and model version give the same answer on the same platform (floating
point differences across hardware can in principle flip a near tied token;
the committed outputs record their platform). The question level corpus
boundary check (RCM/MCM questions) runs before the model, exactly as it
does for the grounded policy, because that check is system policy rather
than model behavior; everything past it is the model's own output,
evaluated as is. Context isolation is pinned by `tests/test_generative.py`,
which verifies the prompt contains nothing but the retrieved chunks, the
question and the fixed instructions.

flan-t5-small is deliberately the smallest instruction model that supports
a meaningful comparison; it keeps the experiment cheap and local, and its
failure modes under the support and citation checks are part of the
evaluation (see the answer metrics table). A larger model can be swapped in
with `--gen-model`; nothing in the default workflow depends on any of
this.

## Evaluation dataset

18 questions in `eval/questions.json` across eight types (numeric,
limitations, definition, enumeration, conditional, factual lookup, and two
comparative variants), plus two controls. Every question carries structured
fields; nothing is parsed out of prose:

- `answerable_from_corpus`: 16 true, 2 false.
- `gold_articles`: explicit article numbers; comparative questions list
  both sides (["85", "86"]); controls have none by definition.
- `related_articles`: near miss articles that do not count against
  citation precision (for q17 the statute boundary control, Art. 86 defines
  the offense whose punishment the question asks about).
- `must_include`: statutory phrases a complete answer must contain, each
  validated against the gold articles' text by `eval/validate_dataset.py`,
  so the answer key itself is verified to be satisfiable from the corpus.

Accounting used everywhere in this README, the code and the output files:
**16 retrieval scored answerable questions, 1 statute boundary control
(q17), 1 pure out of corpus control (q18)**. Controls are excluded from
retrieval metrics and scored on refusal behavior.

## Evaluation rubric

The full rubric is `eval/rubric.md`. In brief: retrieval metrics (hit@1/3/5,
MRR, all gold retrieved) are computed over the 16 answerable questions;
answer metrics (refusal correctness, citation hit, citation precision,
content coverage, support, numbers supported) are computed for every
question; the two controls are scored purely on refusal behavior.
Extractive support is a verbatim gate; generative support is a clearly
labelled fuzzy alignment heuristic (70 percent content token recall per
sentence plus every stated digit present in the retrieved text), which
catches novel wording and fabricated figures but cannot catch a fluent
recombination of retrieved words into a claim the statute does not make.
Automatic metrics and qualitative interpretation are kept separate, here
and in the rubric.

## Main results

All tables printed by `eval/build_summary.py` from the files in `outputs/`;
no number in this README is hand computed. The bm25/tfidf/stem rows are the
default install; the dense, hybrid and generative rows are the committed
outputs of the optional extras.

### Retrieval (16 scored questions; identical answer layer)

| run | hit@1 | hit@3 | hit@5 | MRR | missed | all gold in top k |
| --- | --- | --- | --- | --- | --- | --- |
| bm25_baseline | 9/16 | 11/16 | 11/16 | 0.62 | 5/16 | 10/16 |
| bm25_cw120 | 9/16 | 11/16 | 11/16 | 0.61 | 5/16 | 10/16 |
| bm25_cw300 | 10/16 | 11/16 | 11/16 | 0.66 | 5/16 | 10/16 |
| tfidf_baseline | 9/16 | 9/16 | 10/16 | 0.58 | 6/16 | 9/16 |
| bm25_stem | 9/16 | 10/16 | 12/16 | 0.62 | 4/16 | 11/16 |
| dense_baseline | 12/16 | 13/16 | 14/16 | 0.79 | 2/16 | 13/16 |
| hybrid_baseline | 10/16 | 12/16 | 14/16 | 0.71 | 2/16 | 14/16 |

### Answer metrics by configuration

Each column reports retrieval and answers for the same run; nothing is
mixed across configurations. hybrid_generative shares hybrid_baseline's
retrieval by construction.

| metric | bm25_baseline | dense_baseline | hybrid_baseline (best measured) | hybrid_generative |
| --- | --- | --- | --- | --- |
| answered (of 16 answerable) | 13 | 15 | 15 | 16 |
| refusals on answerable questions | 3 | 1 | 1 | 0 |
| controls refused correctly | 2/2 | 2/2 | 2/2 | 1/2 |
| answers citing a gold article | 11/13 | 13/15 | 13/15 | 0/16 |
| mean citation precision | 0.577 | 0.689 | 0.767 | 0.0 |
| mean content coverage | 0.692 | 0.778 | 0.767 | 0.156 |
| support check passed | 18/18 | 18/18 | 18/18 | 16/18 |
| answers with every stated number in context | 13/13 | 15/15 | 15/15 | 16/17 |

The bm25 false refusals are q05, q06 and q07, all questions whose gold
article never reached the top k: the grounded policy converts retrieval
misses into refusals rather than wrong answers, which is the designed
behavior, and hybrid retrieval converts two of the three (q05, q06) into
correct answers. The remaining hybrid refusal (q07) and its two answers
citing no gold article (q04, q12) get their own failure entries below. The
generative column is read in its own section under the ablations. Per
question detail for every configuration is in
`outputs/evaluation_report.md` and `outputs/question_results.csv`.

### Per question first gold rank

| question | bm25 | tfidf | bm25_stem | dense | hybrid |
| --- | --- | --- | --- | --- | --- |
| q01 | 1 | 1 | 1 | 1 | 1 |
| q02 | 1 | 1 | 1 | 1 | 1 |
| q03 | 1* | 1* | 1* | 1* | 1* |
| q04 | MISS | MISS | 1 | 1 | MISS |
| q05 | MISS | MISS | MISS | 1 | 5 |
| q06 | MISS | MISS | MISS | MISS | 2 |
| q07 | MISS | MISS | MISS | 2 | 2 |
| q08 | 1 | 1 | 1 | 5 | 5 |
| q09 | 1 | 1 | 1 | 1 | 1 |
| q10 | 2 | 4 | 4 | 1 | 1 |
| q11 | 1 | 1 | 1 | 1 | 1 |
| q12 | MISS | MISS | MISS | MISS | MISS |
| q13 | 2 | MISS | 2 | 1 | 1 |
| q14 | 1! | 1! | 1! | 1! | 1* |
| q15 | 1 | 1 | 1 | 1 | 1 |
| q16 | 1 | 1 | 4 | 1 | 1 |

multi gold questions: * = all gold articles retrieved, ! = only one side
retrieved. (Chunk size columns omitted here for width; the full seven
column table is printed by `eval/build_summary.py`.)

## Cases where it worked, and why

**q01, punishment for premeditated murder.** Art. 118 takes the top ranks
under every retriever; "premeditated" is a rare term concentrated in
exactly one article, and Art. 118 is one of the few punitive articles whose
punishment is fixed in the statute itself. The constructed answer quotes
the murder definition and the operative clause, and the content check
confirms "death or imprisonment for life" verbatim. When the query shares a
distinctive content word with one short provision, sparse retrieval plus
extraction is extremely hard to beat.

**q03, desertion versus AWOL.** The near duplicate comparative pair: Art.
85 and Art. 86 are both retrieved (marked * in the per question table), and
the comparative selection rule reserves answer slots per article, so the
answer quotes Art. 86's absence prongs and Art. 85's "intent to remain away
therefrom permanently", which is the legal dividing line. The structurally
identical q14 fails on one side under every sparse configuration, and the
contrast between the two is the most instructive pair in the eval.

**q09, the three types of courts-martial.** Retrieval is trivial (distinct
title vocabulary), but this is the enumeration completion showcase: the
selected sentence introduces the list and ends mid enumeration, and the
builder pulls items (1) through (3) back in whole. Content coverage 3/3
(general, special, summary). Without list completion this answer names one
kind of court-martial and silently narrows the law.

**q11, the Article 15 question.** The famous colloquial name works for two
stacked reasons: the query literally names the article, so both the named
article scoring bonus and the router guarantee Art. 15 presence, and all
five retrieved chunks are parts of Art. 15. The answer quotes the operative
"may impose one or more of the following disciplinary punishments" clause
with items. Compare q17 below, where Art. 15's very same day limit tables
become the trap.

**q05/q06 under hybrid retrieval, the definitional recoveries.** "How does
the UCMJ define a commanding officer" and "who counts as an accuser" are
refused under every sparse configuration (the definitions live in Art. 1,
invisible behind ubiquitous usage of the defined terms). Dense retrieval
surfaces Art. 1 for q05; fusion surfaces it for q06 at rank 2 even though
neither BM25 nor dense alone has it in the top 5. Both answers then quote
the actual definitional clause, because the definitional gate in the
support test requires a definitional clause rather than a usage. q05 is
Example 1 below, in full.

**q17 and q18, the controls.** The statute boundary question ("maximum
confinement for AWOL over 30 days") is refused with the Manual for
Courts-Martial explanation, and the Rules for Courts-Martial question is
recognised as out of corpus before retrieval is consulted. Both behaviors
are pinned by tests. These only look easy; the policy ablation below shows
what the same retrieval produces without the guardrails.

## Where it failed, and why

**Failure 1: morphology. "Deserter" never matches "desertion" (q04).** The
query "Can a deserter be sentenced to death?" scores zero against Art. 85
under BM25, because the statute says "desertion" and "deserts" while the
query says "deserter", and a bag of words matcher treats those as unrelated
tokens. The retriever chases "sentenced to death" into capital procedure
articles instead, and the grounded answer quotes Art. 25a (capital panel
size) with a citation the scorer flags as wrong: support was satisfied by
"sentenced" and "death", which establish topic but not entailment. Stemming
fixes the retrieval (rank 1 in `bm25_stem`) and dense fixes it without
stemming's side effects; hybrid fusion then loses it again (Example 3 and
the ablations), so the failure is reported for both tiers rather than
hidden.

**Failure 2: boilerplate kills the query's key terms (q05, q07).** "Who is
subject to the UCMJ?" should retrieve Art. 2, which enumerates exactly
that. It never surfaces under any sparse configuration. The phrase "subject
to this chapter" appears in 97 of the 198 provisions, so the words carrying
the question's meaning are the least informative words in the corpus. Same
mechanism for "commanding officer" (q05): defined once in Art. 1, used in
dozens of provisions, the definition invisible behind the usage. These
misses are unchanged at every chunk size and under both sparse retrievers,
the fingerprint of a term statistics problem. Dense retrieval fixes both
retrievals; under hybrid, q05 is answered correctly while q07 remains a
false refusal for a selection layer reason: the Art. 2 chunk hybrid
retrieves at rank 2 is a later part of the article that lacks the "the
following persons are subject" clause the enumeration gate requires. Right
article, wrong chunk part, safe refusal instead of a partial answer.

**Failure 3: the corpus does not speak the user's language (the "UCMJ"
trap).** The statute never calls itself the UCMJ; the token appears in two
provisions, one being Art. 146, the Military Justice Review Panel. For any
question phrased "under the UCMJ", that token is corpus rare, high idf and
points at the wrong article. The permissive q07 answer makes the trap
concrete: it confidently quotes Art. 146 panel term limits for "who is
subject to the UCMJ". The vocabulary users add for clarity is exactly the
vocabulary that misleads a term statistics retriever, and no term weighting
scheme can learn that "UCMJ" means "this chapter". That is a falsifiable
claim, and the dense ablation tests it.

**Failure 4: a provision whose name is generic is structurally invisible
(q14).** "Conduct unbecoming an officer versus the general article": Art.
133 retrieves at rank 1 on its distinctive title, and Art. 134 never
appears under any sparse configuration, because its name is made of two of
the most generic words in the corpus. One side of the comparison is
grounded, the other missing, and the answer metrics show it (content 1/2,
"all gold" failed). Together with q03, the same question shape succeeding
when both provisions have distinctive names, this isolates the variable
cleanly. Only the hybrid retriever covers both sides; that recovery is
Example 2 below, in full.

**Failure 5: right article, wrong paragraph (q02).** Art. 43 fills the top
ranks, the citation is right, and the content check still fails: the
operative five year clause never says "statute of limitations" in its body
(the tolling and extension clauses do), so within a pool of near tied Art.
43 sentences the operative clause loses the tie on chunk rank and misses
the three slots. Retrieval granularity has a selection layer twin: heading
vocabulary beats operative vocabulary. The same mechanism truncates q11's
enumeration (the correctional custody items live in an unretrieved chunk
part) and q16 picks up an off topic Art. 63 tagalong that legitimately
shares "order" and "regulation", which is what a lexical support test
cannot distinguish and why citation precision is reported.

**Failure 6: not representational at all (q12).** "What has to happen
before charges can be referred?" misses under every retriever including
dense and hybrid, because the answer (the Art. 32 preliminary hearing) is
phrased in procedural language spread across half a dozen referral
provisions and the question shares no distinctive anchor with any of them.
Retrieval upgrades fix retrieval shaped failures and nothing else; knowing
which failures are which is what the eval is for.

## The main controlled change: answer policy (grounded versus permissive)

One thing changes: the answer policy in `rag/answer.py`. Retrieval is held
fixed at the BM25 baseline, both arms run the same deterministic extractive
code, and the permissive arm simply skips the support test, the punitive
article guard and the corpus boundary checks. Nothing is staged; the
permissive failures below are whatever the code produced. Full record in
`outputs/answer_policy_ablation.json`.

| metric | grounded | permissive |
| --- | --- | --- |
| refusals on answerable | 3 | 0 |
| controls refused correctly | 2/2 | 0/2 |
| answers citing a gold article | 11/13 | 11/16 |
| mean citation precision | 0.577 | 0.469 |

What the permissive policy actually does on the questions that matter:

| question | grounded | permissive |
| --- | --- | --- |
| q17 max confinement, AWOL over 30 days | Manual for Courts-Martial boundary refusal | quotes Art. 15's non-judicial punishment day limits, cited to Art. 15 |
| q18 RCM 707 speedy trial | out of corpus refusal | quotes Art. 6b victim rights enforcement text |
| q05 define commanding officer | refusal | quotes a usage of "commanding officer" from Art. 23 |
| q07 who is subject to the UCMJ | refusal | quotes Art. 146 review panel term limits |

q17 deserves the close look, and it is reproduced in full as Example 4
below. The query's "30 days" matches real day limit tables in Art. 15, so
the retrieved context is full of true, plausible looking numbers that
belong to a different legal instrument entirely (non judicial punishment
limits, not court-martial confinement maxima). The permissive policy quotes
them with a confident citation. Note what the extractive layer cannot do:
it cannot produce the MCM's actual one year figure, because that number
exists nowhere in the corpus. A generative model can supply a figure its
context never contained, which reads better and is epistemically worse; the
generative experiment demonstrates exactly that on this question (it
answers "a minimum of six months", a figure found nowhere in the retrieved
chunks, flagged by the support check). The extractive permissive failure
mode is misattribution with a real citation attached, which for a legal
user may be the most dangerous shape of all: every quoted sentence is
genuine law, verbatim, and the composition is still wrong.

This is the single most important thing the project demonstrates. The
grounded policy's guardrails (support threshold, punitive article guard,
boundary checks) are the only difference between "safely refuses on its
retrieval misses" and "answers all eighteen questions, citing the wrong
legal instrument on the two questions engineered to punish it".

## Before-and-after examples

Complete examples, each changing exactly one component, generated from the
committed run files by `eval/build_before_after.py` and written to
`outputs/before_after.md` (which also contains a fifth example, q04 BM25 to
dense). Nothing here is hand written; the retrieved lists show all five
chunks in rank order.

### Example 1: q05, one change: BM25 to hybrid retrieval

**Question.** How does the UCMJ define a commanding officer? **Gold.**
Article 1. Same chunks, same k, same grounded answer layer.

Before (bm25_baseline), retrieved:

1. Art. 22, Who may convene general courts-martial (score 8.9771)
2. Art. 23, Who may convene special courts-martial (score 8.6007)
3. Art. 23, Who may convene special courts-martial (score 8.1268)
4. Art. 22, Who may convene general courts-martial (score 7.8403)
5. Art. 139, Redress of injuries to property (score 7.3769)

Answer before:

> The retrieved provisions do not answer this question. Rather than
> construct an answer from weakly matching passages, the system declines.

Scores before: first gold rank MISS, refused, refusal incorrect (the
question is answerable).

After (hybrid_baseline), retrieved:

1. Art. 22, Who may convene general courts-martial (score 0.0325)
2. Art. 23, Who may convene special courts-martial (score 0.0315)
3. Art. 22, Who may convene general courts-martial (score 0.0299)
4. Art. 23, Who may convene special courts-martial (score 0.0294)
5. Art. 1, Definitions (score 0.0286)

Answer after:

> (3) The term " commanding officer " includes only commissioned officers.
> (4) The term " officer in charge " means a member of the Navy, the Marine
> Corps, or the Coast Guard designated as such by appropriate authority.
> (5) The term " superior commissioned officer " means a commissioned
> officer superior in rank or command. (Art. 1, UCMJ)

Scores after: first gold rank 5, answered, citation hit, citation precision
1.0, content coverage 1.0, support ok.

Why it changed: "commanding officer" is used in dozens of provisions and
defined in one; term statistics cannot see the definition behind the usage,
but the dense encoder ranks Art. 1 (Definitions) high for a "how is X
defined" query, fusion carries it into the top 5, and the definitional gate
then selects the actual definitional clause. The related q07 is the
documented counterexample where retrieval recovery alone does not flip the
answer (right article, wrong chunk part).

### Example 2: q14, one change: BM25 to hybrid retrieval

**Question.** What is the difference between conduct unbecoming an officer
and the general article? **Gold.** Articles 133 and 134 (both required).

Before (bm25_baseline), retrieved:

1. Art. 133, Conduct unbecoming an officer (score 17.0274)
2. Art. 142, Judges (score 11.1909)
3. Art. 4, Dismissed officer's right to trial by court-martial (score 9.1428)
4. Art. 142, Judges (score 8.8190)
5. Art. 130, Stalking (score 8.6597)

Answer before (abridged to its first and second passages; full text in
`outputs/before_after.md`):

> Any commissioned officer, cadet , or midshipman who is convicted of
> conduct unbecoming an officer shall be punished as a court-martial may
> direct. (Art. 133, UCMJ)
>
> A senior judge shall be paid for each day on which he performs judicial
> duties with the court an amount equal to the difference between— (A) the
> daily equivalent of the annual rate of pay provided for a judge of the
> court; and (B) the daily equivalent of the annuity of the judge under
> section 945 of this title (article 145) [...] (Art. 142, UCMJ)

Scores before: first gold rank 1, all gold retrieved False, citation
precision 0.333, content coverage 0.5. One side of the comparison is
grounded; the "difference between" phrasing dragged in Art. 142's pay
difference clause as a tagalong.

After (hybrid_baseline), retrieved:

1. Art. 133, Conduct unbecoming an officer (score 0.0328)
2. Art. 91, Insubordinate conduct toward warrant officer, noncommissioned officer, or petty officer (score 0.0289)
3. Art. 37, Command influence (score 0.0280)
4. Art. 134, General article (score 0.0279)
5. Art. 60a, Limited authority to act on sentence (score 0.0266)

Answer after (abridged; full text in `outputs/before_after.md`):

> Any commissioned officer, cadet , or midshipman who is convicted of
> conduct unbecoming an officer shall be punished as a court-martial may
> direct. (Art. 133, UCMJ)
>
> Though not specifically mentioned in this chapter, all disorders and
> neglects to the prejudice of good order and discipline in the armed
> forces, all conduct of a nature to bring discredit upon the armed forces,
> and crimes and offenses not capital [...] shall be punished at the
> discretion of that court. (Art. 134, UCMJ)

Scores after: first gold rank 1, all gold retrieved True, citation
precision 0.667, content coverage 1.0.

Why it changed: Art. 134's title ("General article") is made of two of the
most generic words in the corpus, invisible to term statistics; the dense
encoder matches it semantically, fusion puts it at rank 4, and the
comparative selection rule then quotes both sides. An Art. 60a tagalong
still costs precision, which the metric reports.

### Example 3: q08, one change: extractive to generative answerer

**Question.** What rights does Article 31 give a servicemember suspected of
an offense? **Gold.** Article 31. Retrieval identical in both arms (hybrid:
four Art. 6b chunks at ranks 1 to 4, Art. 31 at rank 5 via the article
router).

Answer before (hybrid_baseline, grounded extractive; abridged, full text in
`outputs/before_after.md`):

> (B) A preliminary hearing under section 832 of this title (article 32)
> relating to the offense. [...] (Art. 6b, UCMJ)
>
> (b) No person subject to this chapter may interrogate, or request any
> statement from, an accused or a person suspected of an offense without
> first informing him of the nature of the accusation and advising him that
> he does not have to make any statement regarding the offense of which he
> is accused or suspected and that any statement made by him may be used as
> evidence against him in a trial by court-martial. (Art. 31, UCMJ)

Scores before: citation hit True, citation precision 0.5, content coverage
0.5, support ok (verbatim).

Answer after (hybrid_generative, flan-t5-small):

> The right to be reasonably protected from the accused.

Scores after: citation hit False, citation precision 0.0, content coverage
0.0, support ok True (the sentence is aligned with the retrieved Art. 6b
text; it is faithful to the wrong provision).

Why it changed: the extractive layer, forced to attach citations and
scoring sentences against the question, surfaces the Art. 31 warning clause
even from rank 5 (with an Art. 6b tagalong that costs precision). The
generative model, given the same context dominated by victim rights text,
summarises the dominant content and answers about the victim, not the
suspect, with no citation. The support check passes because the claim is
genuinely in the retrieved context; support measures faithfulness to
retrieval, not correctness for the question, and this example is why both
citation metrics and support are reported.

### Example 4: q17, one change: grounded to permissive policy

**Question.** What is the maximum confinement for being AWOL for more than
30 days? **Expected behavior.** Boundary refusal: the true figure lives in
the Manual for Courts-Martial, not the statute. Retrieval identical in both
arms (three Art. 15 chunks, Art. 48, Art. 57).

Answer before (grounded):

> The statute does not contain this figure. The UCMJ typically provides
> punishment 'as a court-martial may direct'; the specific maximum
> punishments are set in the Manual for Courts-Martial punishment tables,
> which are not part of this corpus.

Scores before: refused True, refusal correct True.

Answer after (permissive; abridged, full text in
`outputs/before_after.md`):

> (b) Subject to subsection (a), any commanding officer may [...] impose
> one or more of the following disciplinary punishments for minor offenses
> without the intervention of a court-martial— (1) upon officers of his
> command— (A) restriction to certain specified limits [...] for not more
> than 30 consecutive days; [...] (Art. 15, UCMJ)
>
> (b) Punishment.— The punishment for contempt under subsection (a) may not
> exceed confinement for 30 days, a fine of $1,000, or both. (Art. 48,
> UCMJ)

Scores after: refused False, refusal correct False.

Why it changed: the query's "30 days" matches genuine day limit tables in
Art. 15 (non-judicial punishment) and Art. 48 (contempt), so without the
punitive article guard and the boundary check the permissive policy quotes
real, verbatim, correctly cited law that answers a different legal question
entirely. For comparison, the generative experiment on this same question
invents "a minimum of six months", a figure found nowhere in the retrieved
chunks, which the fuzzy support check flags; the two failure shapes
(misattribution versus fabrication) are exactly what the separate support
and citation metrics are designed to distinguish.

## Additional ablations

### Chunk size (120 / 180 / 300)

Chunk size moves little (hit@1 9, 9, 10), and the reason is worth stating:
most UCMJ provisions are shorter than one chunk at any tested size, so
re-chunking only redistributes the handful of long articles. At 300 words
q13 improves to rank 1: Art. 88 is a single chunk at every size, but its
competitors are long articles whose consolidation at 300 words changes
their length normalisation. The important negative result: the q05/q07/q12
misses do not move at any size, confirming term statistics rather than
chunk boundaries as the cause. Within a long article, chunking still
decides which paragraph you get; q02 and q11 above are the demonstration.

### Retriever (TF-IDF cosine)

Slightly worse than BM25 across the board (hit@3 drops to 9, q13 becomes a
miss), and the two sparse retrievers agree on every hard failure, which is
expected: both are bag of words scorers with the same blind spots. Swapping
between two sparse retrievers is a lateral move; the misses need a
different representation, not a different formula over the same terms.

### Tokenisation (light stemming), the found and fixed and paid for one

`--stem` folds deserter/desertion and fixes q04 at rank 1, exactly as
designed. The same folding demotes q10 (officer collisions promote
competing articles past Art. 25a, rank 2 to 4) and q16 (rank 1 to 4).
Aggregate hit@1 unchanged at 9, misses down from 5 to 4. A targeted gain, a
diffuse cost, netting out near zero: the honest shape of most retrieval
interventions, and the eval set is what tells you whether the trade is
worth it. The answer layer sidesteps the collision problem with plural only
folding, which fixes nothing morphological but breaks nothing either.

### Dense retrieval, testing the failure analysis on its own prediction

Failures 2 and 3 above claim their misses cannot be fixed by any term level
intervention, only by a representation that knows synonymy. The dense run
(all-MiniLM-L6-v2) confirms it: q05 rank 1, q07 rank 2, q04 rank 1 without
stemming and without its collisions, hit@1 12/16, misses down to 2. The
prediction held.

What dense pays: on q08, "What rights does Article 31 give...", the
encoder's five nearest chunks are all Art. 6b, rights of the victim of an
offense. To the encoder, rights of a suspect and rights of a victim are
nearly the same sentence, and the one token that pins the question down,
"31", is just another subword. This is where the article number router
earns its place: it swaps Art. 31's best chunk into the last slot, so the
dense configuration answers correctly from rank 5 instead of missing
outright. The symmetry stands: dense wins when the user's words are not the
statute's words and loses when the user's words were the statute's words
all along, and in a legal corpus the exact identifier is often the entire
meaning of the question.

### Hybrid fusion, the best measured configuration's trade

Hybrid RRF is the only configuration that retrieves both sides of both
comparative questions (all gold 14/16, q14 finally covered) and it recovers
q06 (Art. 1 sat high enough in both top 50 lists that fusion lifts it to
rank 2 when neither retriever alone had it in the top 5). Its consensus
bias also costs it q04, where dense alone was simply right and fusion let
mediocre chunks present in both lists outvote a single confident list.
Dense is the best single retriever on aggregate rank metrics; hybrid trades
those peaks for the best coverage floor, which is why it is the
configuration the measurements favor; and q12 misses under all of them,
because its failure was never representational.

### The generative experiment, read honestly

flan-t5-small over hybrid retrieval answers all 16 answerable questions and
one of the two controls, cites a gold article in 0 of 16 answers (it
ignores the citation instruction entirely at this scale), reaches content
coverage 0.156, and produces two answers the fuzzy support check flags,
including an invented "six months" punishment figure on q17. Some
individual answers are genuinely good extractions (its q01 murder answer
quotes the operative clause almost verbatim; its q09 answer lists all three
court-martial types), which is what makes the aggregate numbers
informative rather than a strawman: a small generator can read the
retrieved context, but it does not follow citation discipline and it fills
gaps with fluent text when the context runs out. These results bound what
an 80M parameter local model does under this harness; they are not a claim
about generation in general, and the interface accepts larger models via
`--gen-model`. Within this project's constraints, they are the measured
justification for keeping the deterministic extractive synthesizer as the
default answer layer.

## Honest limitations

- The extractive support test is lexical. Two common shared tokens can
  admit an off topic sentence (q16's Art. 63 tagalong), and topical overlap
  can pass on the wrong article when retrieval missed the right one (q04,
  q12). Mean citation precision 0.577 baseline / 0.767 hybrid is the honest
  price, reported rather than hidden. Entailment, not overlap, is the real
  fix, and the small local generator evaluated here is nowhere near capable
  of being that judge.
- The generative support check is a fuzzy alignment heuristic (token recall
  plus digit presence). It catches novel wording and fabricated figures; it
  cannot catch a fluent recombination of genuinely retrieved words into a
  claim the statute does not make, and spelled out numbers evade the digit
  check (q17's "six months" was caught by token recall, not the number
  check). Generated answers with citations are not therefore fully
  supported, and this README never counts them as such.
- Being extractive, default answers can be stilted: statutory clauses
  stitched with citations, not prose. The upside is that faithfulness to
  the retrieved context is structural (18/18 verbatim, independently re
  verified), not behavioral. Faithfulness to context is not legal
  correctness, and neither implies completeness; the metrics keep them
  separate.
- Enumeration completion cannot cross into chunks retrieval never returned
  (q11 truncation), and paragraph selection within a provision inherits
  retrieval's near tie ordering (q02, q07 under hybrid).
- `must_include` content coverage samples representative phrases; it is a
  floor on completeness, not a full semantic grade. The dataset validator
  guarantees every phrase is achievable from the gold articles.
- 18 questions surface failure modes; they are not enough for stable
  metrics, and single question flips move the aggregates. The dense margins
  (12 versus 9 at hit@1) are directionally clear but small sample. The
  stemming and stopword changes were tuned against this same question set,
  which is the standard small eval risk: fixes are validated on the set
  that motivated them.
- The dense results come from one small general purpose encoder on one
  question set; a legal domain encoder might behave differently on the
  identifier failure. The generative results come from the smallest usable
  instruction model and bound only what that scale does.
- The corpus is the statute only. A production military law assistant would
  need the MCM (punishment tables, Rules for Courts-Martial), and the two
  controls exist to prove the system knows where its corpus ends. This is
  an engineering demonstration, not legal advice.

## Reproducibility

- `./run_all.sh` regenerates every run file, the answer policy ablation,
  answer scores for every configuration present, `outputs/summary.json`,
  the per question report and the before/after examples, and validates the
  dataset first. With only the default install, the dense/hybrid and
  generative stages are skipped with a notice and the committed outputs for
  them remain in place.
- Provenance of the committed outputs: every file in `outputs/` was
  regenerated for this submission. The baseline, chunking, tfidf, stemming
  and policy ablation runs were produced with the default install; the
  dense, hybrid and generative runs were produced with the optional extras
  installed (Python 3.12.13, exact package versions in
  `requirements-lock.txt`). Every output file carries its full
  configuration (retriever, answerer, chunking, k, policy, answer builder
  version, python version); no timestamps, so identical runs diff
  identically.
- The sparse pipeline and the extractive answerer are deterministic: same
  inputs, same outputs, byte for byte. Dense embeddings and greedy decoding
  are deterministic for a given model version on a given platform; across
  hardware, floating point differences can in principle move a near tied
  rank or token, which is stated here rather than papered over. There is no
  API, no key, no sampled randomness anywhere, and no network at eval time
  (the optional extras download their models once, on first use).
- Dependencies: `requirements.txt` (default baseline), `requirements-dev.txt`
  (pytest), `requirements-dense.txt` and `requirements-gen.txt` (optional
  extras), `requirements-lock.txt` (exact versions of the environment that
  produced the committed optional runs).
- CI (`.github/workflows/ci.yml`) installs the default requirements from
  scratch on every push, validates the dataset, runs the full test suite,
  runs the baseline evaluation and checks the output schemas; a manually
  triggered job does the same for the optional extras with model caching.
- `python3 -m pytest tests/ -q`: 58 tests covering chunk/provision
  integrity, retrieval regressions, dataset schema, multi article credit,
  refusal behavior, boundary recognition, exact number preservation,
  verbatim extraction, RRF fusion and its tie break, dense embedding shape
  and normalisation, generative prompt isolation and citation parsing,
  helpful failure without optional dependencies, report and before/after
  generation, artifact schemas, CLI behavior and determinism. On the
  default install the dense and generative model tests skip themselves (51
  pass, 7 skip); with the extras all 58 run.
- `make setup`, `make setup-extras`, `make test`, `make eval`, `make all`
  wrap the same commands.

## Repository layout

```
data/fetch_corpus.py     fetches the UCMJ from Cornell LII (provenance only,
                         the corpus is committed)
data/corpus/             198 provisions, one file each, + manifest.json
rag/chunk.py             ingestion and paragraph aware chunking
rag/index.py             BM25 (from scratch), TF-IDF cosine embeddings,
                         light stemmer, optional dense (MiniLM) and hybrid
                         (RRF) retrievers
rag/answer.py            grounded extractive answer synthesis: sentence
                         splitting, scoring, support test, enumeration
                         completion, refusals and corpus boundaries
rag/generate.py          optional local generative answerer (flan-t5-small),
                         context isolated prompt, citation parsing
rag/pipeline.py          chunk -> index -> retrieve -> route -> answer
eval/questions.json      18 questions with structured gold fields
eval/rubric.md           the scoring rubric (automatic + interpretation)
eval/validate_dataset.py schema and groundedness checks for the dataset
eval/run_eval.py         runs the pipeline, computes retrieval metrics
eval/score_answers.py    automatic answer metrics from a run file
eval/run_answer_policy_ablation.py  grounded vs permissive, retrieval fixed
eval/build_summary.py    builds outputs/summary.json and the README tables
eval/build_report.py     per question report (md + csv), rule based failure
                         categories
eval/build_before_after.py  complete before/after examples from run files
outputs/                 all runs, scores, ablation, summary, report, csv,
                         before/after examples
tests/                   pytest suite (58 tests)
run_all.sh               reproduce everything
Makefile                 setup / test / eval / all
.github/workflows/ci.yml clean clone CI (default on push, extras on demand)
```
